json.extract! ambassador, :id, :first_name, :last_name, :email, :phone, :address, :citizenship, :presentation, :position, :comment, :reason_of_choice, :territoire, :created_at, :updated_at
json.url ambassador_url(ambassador, format: :json)
